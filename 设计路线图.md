# DayGlyph v2 顺序实施路线图

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:writing-plans before turning any phase below into an executable implementation plan. Do not execute phases in parallel. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把产品部 2026-06-16 的 DayGlyph v2.0 正式文档拆成可逐个交付、可逐个验收的 iOS 实施阶段。

**Architecture:** 先建立 v2 数据与导航骨架，再逐个替换用户可见模块。每个阶段只产出一组可演示页面和对应测试，不把后续模块的复杂逻辑提前塞入当前阶段。

**Tech Stack:** SwiftUI、SwiftData、Swift Testing、Foundation Models 现有统一分析入口、UserNotifications、后续 Universe 阶段使用 RealityKit/RealityView。

---

## 范围判断

正式产品文档覆盖四个一级模块、二十多个页面、数据迁移、视觉系统、通知、分享、无障碍和 Demo 脚本。它不是一个可一次性执行的单计划。当前仓库仍是旧版三 Tab：`今日`、`月历`、`设置`，并且用户可见结果仍是 Glyph。因此必须按下面顺序推进。

硬约束：

- [ ] 任一阶段未通过验收前，不启动下一阶段实现。
- [ ] 任一阶段只允许修改它声明的文件范围；跨阶段依赖只能通过稳定模型、服务或组件接口暴露。
- [ ] 每个阶段完成后跑单元测试；涉及 UI 的阶段至少在 393 pt 基准宽度和 375 pt 小屏宽度人工走查。
- [ ] 所有新增用户可见文案使用简体中文，且不得出现医疗诊断、疗效承诺、惩罚性签到文案。

## 阶段 1：v2 基础架构与 Today 核心闭环

详细计划文件：`docs/superpowers/plans/2026-06-17-dayglyph-v2-foundation-and-today-core.zh-CN.md`

目标页面：

- T-00 今日空状态
- T-01 情绪记录输入
- T-02 分析与生成
- T-03 情绪鸡尾酒结果
- 四 Tab 应用壳，但 Universe/Echo/Mine 只提供稳定占位和后续入口，不实现业务闭环

交付标准：

- [ ] 底部导航从三项替换为 `今日`、`宇宙`、`回声`、`我的`。
- [ ] Today 主路径完成“记录 -> 调制 -> 鸡尾酒结果”，用户界面不再出现“一划”、`Glyph`、百分比严重度。
- [ ] 每条记录保存稳定的 `EmotionRecipe`、`CocktailVisual`、`PlanetVisual`、`MoodWeather` 派生数据或等价可复现字段。
- [ ] 旧 `glyphSeed` 只作为兼容字段，不再进入主界面。
- [ ] 现有分析服务继续作为统一入口，UI 不暴露 Foundation Models 品牌。

不做：

- [ ] 不实现完整宇宙 3D。
- [ ] 不实现微行动状态机。
- [ ] 不实现回声、时间来信、共情海。
- [ ] 不实现成就和统计。

## 阶段 2：Today 支持模块

待阶段 1 验收后再生成执行级计划。

目标页面：

- T-04 情绪天气与名言
- T-05 今天迈一小步
- T-06 时间来信
- T-07 共情海入口与发送

建议拆分：

- 2A：微行动候选、偏好过滤、开始/跳过/完成最小闭环。
- 2B：天气/名言和时间来信本地模型。
- 2C：共情海 Demo 本地副本、审核中、固定回应和举报状态。

前置条件：

- [ ] 阶段 1 的 `EmotionRecipe`、`MoodWeather`、`PlanetVisual` 字段稳定。
- [ ] Today 页面结构已经拆成独立 View，不再把所有状态塞进一个大文件。

## 阶段 3：Universe 情绪宇宙

待阶段 2 验收后再生成执行级计划。

**状态：已于 2026-06-18 完成。** 按用户要求直接依据已确认的 Stage 3 设计规格实施，未另建执行级计划。

目标页面：

- U-00 宇宙空状态
- U-01 月轨道宇宙总览
- U-02 月星球详情
- U-03 日期落点摘要
- U-04 宇宙趋势与统计

建议拆分：

- 3A：月级聚合模型、2D 可访问降级列表、日期摘要。
- 3B：RealityKit 月星球原型、稳定 seed、减少动态。
- 3C：趋势与统计、导出静态图、性能优化。

前置条件：

- [x] 阶段 1 保存的日星球参数可稳定聚合。
- [x] 阶段 2 不再改动 DayEntry 基础字段语义。

交付结果：

- [x] 3A：月级聚合模型、2D 可访问列表、月详情与日期摘要。
- [x] 3B：RealityKit 月星球、稳定 seed、日期光点命中与减少动态降级。
- [x] 3C：月/季/年趋势、可读表格替代、静态图片导出与性能收敛。

## 阶段 4：Echo 回声与通知

**状态：已于 2026-06-18 完成。** Stage 4 与 Stage 5 按用户要求合并实施，执行计划见 `docs/superpowers/plans/2026-06-18-dayglyph-v2-stage4-stage5.zh-CN.md`。

目标页面：

- E-00 回声首页
- E-02 行动回声反馈弹层
- E-03 我的发现
- 通知：行动回声提醒、时间来信触达、每日记录提醒统一调度

建议拆分：

- 4A：`ActionInstance` 与 `ActionResponse` 数据模型。
- 4B：回声首页和反馈 Sheet。
- 4C：个人发现聚合和通知调度取消规则。

前置条件：

- [x] 阶段 2 的微行动完成状态稳定。
- [x] `ReminderService` 的每日提醒行为已被迁移到可扩展通知服务。

交付结果：

- [x] 行动实例保存稳定的标题、类别、完成与回声提醒时间，行动回应支持四种中性反馈、可选备注和不记录感受。
- [x] 回声首页展示到期、等待和最近回声；同类别至少三次反馈后才生成“我的发现”。
- [x] 每日记录、行动回声和时间来信使用统一通知描述与可取消标识，关闭模块会取消对应通知。

## 阶段 5：Mine、Onboarding 与设计 QA

**状态：已于 2026-06-18 完成。** 与 Stage 4 合并实施并通过编译、安装和启动验收。

目标页面：

- M-00 我的主页
- M-01 情绪成就
- M-02 情绪历史
- M-03 情绪统计
- M-04 设置与隐私
- O-01/O-02/O-03 新手引导
- 全局 G 状态：Toast、Alert、Sheet、删除确认、导出失败

建议拆分：

- 5A：我的主页、设置迁移、演示数据管理。
- 5B：历史、统计、成就和收藏。
- 5C：新手引导、通知权限、最终设计 QA。

前置条件：

- [x] 阶段 1-4 的核心数据对象稳定。
- [x] 删除记录时关联影响已经在各业务模块中明确。

交付结果：

- [x] 我的主页、累计成就、可搜索历史、情绪统计和设置隐私页面完成，昵称与偏好均为本地数据。
- [x] 三屏首次引导支持跳过、隐私说明和最少偏好；只有用户主动开启提醒才请求通知权限。
- [x] 演示记录、行动与回声均带 `isDemo` 标记，单独清除演示数据不影响真实数据。
- [x] 本地 JSON 导出、导出失败提示、危险删除确认和系统通知设置入口完成。

## 总验收口径

- [ ] 90 秒演示路径能完整走通：今日记录 -> 调制 -> 鸡尾酒 -> 宇宙 -> 微行动 -> 回声 -> 共情海 -> 我的隐私。
- [ ] 旧版可见文案 `一划`、`Glyph`、`月历`、`设置` 不再作为一级体验出现。
- [ ] 默认数据本地保存，任何公开副本都经过二次确认。
- [ ] 减少动态、大字体、高对比和 VoiceOver 至少覆盖关键页面。
- [ ] Demo 数据带 `isDemo` 标识，清除演示数据不误删真实记录。
